# docker-lanchonete-back

## Docker do projeto pos-lanchonete-pos:

Os arquivos `Dockerfile`, `docker-compose.yml` e `01-base-lanchonete` estão na raiz do projeto para facilitar o teste do projeto `pos-lanchonete-pos`.
